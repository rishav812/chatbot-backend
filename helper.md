Document Source (PDF in S3 or local)
        ↓
Text Extraction
        ↓
Text Cleaning / Normalization
        ↓
Chunking (token-aware)
        ↓
Store chunks in DB
        ↓
Generate embeddings (parallelizable)
        ↓
Store embeddings in vector table